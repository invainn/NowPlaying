import React, { Component } from 'react';

class Header extends Component {
    render() {
        return (
            <h2 className="header">NowPlaying</h2>
        );
    };
}

export default Header;